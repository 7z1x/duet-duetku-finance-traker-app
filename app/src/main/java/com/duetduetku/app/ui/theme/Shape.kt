package com.duetduetku.app.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val Shapes = Shapes(
    small = RoundedCornerShape(12.dp),   // 0.75rem
    medium = RoundedCornerShape(20.dp),  // 1.25rem (lg)
    large = RoundedCornerShape(32.dp),   // 2rem (xl/2xl)
    extraLarge = RoundedCornerShape(32.dp) // 2rem
)
